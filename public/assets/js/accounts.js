// CRUD - CREATE, READ, UPDATE, DELETE - links to handlebars 
// waits for handlers until DOM is ready
document.addEventListener('DOMContentLoader', (e) => {
    if(e) {
        console.info('DOM loaded');
    }

    // CREATE - use POST request to create a new dog to the system / or notes ... TBD

    // UPDATE - need the classes/id names from front end & setup the eventlisteners 

    // DELETE - grab the elements to delete 


});

